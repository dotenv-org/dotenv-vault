import * as fs from 'node:fs'
import * as sinon from 'sinon'
import {expect} from '@oclif/test'

import {AppendToIgnoreService} from '../../src/services/append-to-ignore-service'

describe('run', async () => {
  beforeEach( () => {
  })

  it('runs', async () => {
  })
})
